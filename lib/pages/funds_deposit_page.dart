import 'package:flutter/material.dart';

class FundsDepositPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Funds Deposit Page'),)
    );
  }
}