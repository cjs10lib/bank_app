import 'package:flutter/material.dart';

class FundsTransferPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Funds Transfer Page'),)
    );
  }
}