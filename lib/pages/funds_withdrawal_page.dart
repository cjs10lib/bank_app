import 'package:flutter/material.dart';

class FundsWithdrawalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Withdrawal Order Page'),),
    );
  }
}