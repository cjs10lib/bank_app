import 'package:flutter/material.dart';

class ProfileImageUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('Profile image upload'),
    ));
  }
}
